# =============================================================================
# FACTORY BUILD - Waveshare ESP32-P4 86 Panel (4" square)
# =============================================================================
# Factory image: web UI assets (www.js + fonts/icons) are fully embedded in flash.
# =============================================================================

substitutions:
  firmware_version: "0.0.0"
  espcontrol_component_url: "file:///config"
  espcontrol_component_ref: "HEAD"

packages:
  core: !include esp32-p4-86.yaml

esphome:
  project:
    name: jtenniswood.espcontrol
    version: "${firmware_version}"

dashboard_import:
  package_import_url: github://jtenniswood/espcontrol/devices/esp32-p4-86/esphome.yaml@main
