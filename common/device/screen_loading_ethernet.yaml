# =============================================================================
# SCREEN: LOADING
# =============================================================================
# Boot screen for Ethernet-only builds. Must be the first page included in
# packages.yaml so LVGL shows it at startup. If Ethernet is not connected after
# the grace period, switches to ethernet_setup_page.
# =============================================================================

globals:
  - id: boot_grace_period
    type: bool
    initial_value: 'true'

lvgl:
  pages:
    - id: loading_page
      bg_color: 0x000000
      scrollable: false
      widgets:
        - obj:
            align: center
            width: $content_width
            height: SIZE_CONTENT
            bg_opa: 0%
            border_width: 0
            pad_all: 0
            scrollable: false
            layout:
              type: flex
              flex_flow: COLUMN
              flex_align_main: center
              flex_align_cross: center
              pad_row: $setup_loading_pad_row
            widgets:
              - label:
                  id: loading_status_icon
                  text_font: $setup_icon_font_id
                  text: $icon_ethernet
                  text_color: 0xFFFFFF
                  text_align: center
              - label:
                  id: loading_status_title
                  text: "Starting up"
                  text_font: $setup_title_font_id
                  text_color: 0xFFFFFF
                  width: $content_width
                  text_align: center
              - label:
                  id: loading_status_label
                  text: "Booting"
                  text_font: $setup_body_font_id
                  text_color: 0xB0B0B0
                  width: $content_width
                  text_align: center
              # Hidden slider ensures LVGL compiles lv_slider for dynamic use
              - slider:
                  hidden: true
                  width: 0
                  height: 0
              # Hidden arc ensures LVGL compiles lv_arc for modal controls.
              - arc:
                  hidden: true
                  width: 0
                  height: 0
    - id: screen_off_page
      bg_color: 0x000000
      scrollable: false

esphome:
  on_boot:
    - priority: -200
      then:
        - lvgl.resume:
        - lvgl.page.show: loading_page
        - lambda: |-
            lv_obj_add_flag(id(temperatures), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(temperature_2), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(temperature_3), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(temperature_4), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(temperature_5), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(temperature_6), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(display_time), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(network_status_button), LV_OBJ_FLAG_HIDDEN);
        - delay: 100ms
        - if:
            condition:
              lambda: |-
                return id(screen_schedule_asleep) ||
                       (id(schedule_enabled).state && !panel_time_or_fallback(id(panel_time).now(), id(homeassistant_time).now()).is_valid());
            then:
              - script.execute: backlight_force_display_off
            else:
              - light.turn_on:
                  id: display_backlight
                  brightness: 90%
                  transition_length: 500ms
    - priority: -100
      then:
        - lambda: |-
            lv_label_set_text(id(loading_status_label), "Connecting to Ethernet");
        - delay: 15s
        - lambda: 'id(boot_grace_period) = false;'
        - if:
            condition:
              lambda: |-
                return ethernet::global_eth_component == nullptr ||
                       !ethernet::global_eth_component->is_connected();
            then:
              - lvgl.page.show: ethernet_setup_page
              - script.execute: setup_screen_dim
